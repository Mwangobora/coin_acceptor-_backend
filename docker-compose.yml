name: coin_acceptor_backend

services:
  backend:
    container_name: coin_acceptor_backend
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "${BACKEND_PORT:-4000}:4000"
    environment:
      APP_NAME: charging-system-api
      NODE_ENV: development
      HOST: 0.0.0.0
      PORT: 4000
      API_PREFIX: api/v1
      FRONTEND_URL: http://localhost:3000
      CORS_ORIGIN: http://localhost:3000
      SWAGGER_ENABLED: "true"
      COOKIE_SECURE: "false"
      COOKIE_SAME_SITE: lax
      DEVICE_AUTH_ENABLED: "false"
      REQUEST_SIZE_LIMIT: 1mb
      JWT_ACCESS_SECRET: development-access-secret-change-me
      JWT_REFRESH_SECRET: development-refresh-secret-change-me
      JWT_ACCESS_TTL: 7d
      JWT_REFRESH_TTL: 30d
      AUTH_MAX_FAILED_ATTEMPTS: 5
      AUTH_LOCK_MINUTES: 15
      AUTH_MIN_PASSWORD_LENGTH: 12
      DATABASE_URL: postgresql://postgres:postgres@database:5432/charging_system?schema=charging_system
      REDIS_URL: redis://redis:6379
      DEVICE_CREDENTIAL_ENCRYPTION_KEY: 0123456789abcdef0123456789abcdef
      DEVICE_HMAC_CLOCK_SKEW_SECONDS: 300
      DEVICE_EVENT_MAX_FUTURE_SECONDS: 300
      CHOKIDAR_USEPOLLING: "true"
    volumes:
      - .:/app
      - backend_node_modules:/app/node_modules
    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  database:
    container_name: coin_acceptor_backend_database
    image: postgres:16-alpine
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-charging_system}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./charging_system_source_of_truth.sql:/docker-entrypoint-initdb.d/001-charging-system-schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    container_name: coin_acceptor_backend_redis
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  backend_node_modules:
